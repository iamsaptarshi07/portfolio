[theme]
# Primary accent for interactive elements
primaryColor = "#d33682"

# Background color for main content area
backgroundColor = "#002b36"

# Background color used for sidebar and most interactive elements widgets
secondaryBackgroundColor = "#586e75"

# coor used for almost all text
textColor = "#fff"